name: Dev Pipeline

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Kodu çek
        uses: actions/checkout@v3

      - name: Docker versiyon kontrol
        run: docker --version

      - name: Docker image build et
        run: docker build -t node-demo:dev .

      - name: Dev deploy simülasyonu
        run: echo "Kod Dev ortamına deploy edildi!"
